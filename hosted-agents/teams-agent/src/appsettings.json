{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Foundry": {
    "ProjectEndpoint": "",
    "CatalogCacheSeconds": 300,
    "ModelDeployment": "gpt-4o",
    "AgentName": "MCP Learn Agent POC",
    "McpServerUrl": "https://learn.microsoft.com/api/mcp",
    "McpServerLabel": "microsoft_learn"
  },

  "Bots": {
    "_doc": "Routes is a JSON array of { AgentName, AppId } injected by bicep as Bots__Routes. Used by JWT middleware (route-bound aud check) and by FicServiceClientCredentialsFactory (allow-list of valid app ids).",
    "Routes": "[]"
  },

  "TeamsApp": {
    "_doc": "Single shared backend app reg. BackendSecret is the only stored secret in the system (used for OBO from /admin and from the Bot Service OAuth Connection). Per-bot outbound auth is secretless via FIC.",
    "BackendAppId": "",
    "BackendSecret": "",
    "IdentifierUri": "",
    "SsoConnectionName": "foundry-sso",
    "TenantId": ""
  },

  "AdminChatAuth": {
    "_doc": "AAD bearer auth on /admin so the UAMI no longer needs Foundry RBAC. ClientId+Secret default to TeamsApp:BackendAppId/BackendSecret if unset.",
    "Enabled": false
  },

  "TeamsTab": {
    "_doc": "Personal Teams chat tab. Uses Teams SSO for this backend API, then OBO to Foundry. PublicOrigin must be the externally reachable HTTPS origin used in generated manifests.",
    "Enabled": false,
    "PublicOrigin": ""
  }
}
