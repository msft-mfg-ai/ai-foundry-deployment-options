<policies>
    <inbound>
        <base />
        <validate-azure-ad-token 
            tenant-id="TENANT_ID" 
            header-name="Authorization" 
            failed-validation-httpcode="401" 
            failed-validation-error-message="Unauthorized. Valid Entra token is required." 
            output-token-variable-name="jwt">
            <audiences>
                <audience>https://cognitiveservices.azure.com</audience>
            </audiences>
            <!--<required-claims>
                <claim name="xms_mirid" match="any">
                    <value>/subscriptions/YOUR-SUBSCRIPTION-ID/resourcegroups/YOUR-RESOURCE-GROUP/providers/Microsoft.CognitiveServices/accounts/YOUR-FOUNDRY-ACCOUNT/projects/YOUR-FOUNDRY-PROJECT</value>
                    or
                    <value>/subscriptions/YOUR-SUBSCRIPTION-ID/resourcegroups/YOUR-RESOURCE-GROUP/providers/Microsoft.ManagedIdentity/userAssignedIdentities/YOUR-USER-ASSIGNED-IDENTITY</value>
                </claim> 
            </required-claims>-->
        </validate-azure-ad-token>
        <!-- Custom claim validation with substring matching -->
        <choose>
            <when condition="@(!((Jwt)context.Variables["jwt"]).Claims.GetValueOrDefault("xms_mirid", "").StartsWith("/subscriptions/SUBSCRIPTION_ID/"))">
                <return-response>
                    <set-status code="401" reason="Unauthorized" />
                    <set-body>Unauthorized. Caller must be an Azure AI resource from subscription SUBSCRIPTION_ID.</set-body>
                </return-response>
            </when>
        </choose>
        <set-variable name="caller-resource-id" value="@(((Jwt)context.Variables["jwt"]).Claims.GetValueOrDefault("xms_mirid", "unknown"))" />
        <!-- Extract foundry/project name from resource ID for cleaner metrics -->
        <!-- Path format: /subscriptions/{sub}/resourcegroups/{rg}/providers/Microsoft.CognitiveServices/accounts/{foundry}/projects/{project} -->
        <set-variable name="caller-name" value="@{
            var callerId = (string)context.Variables["caller-resource-id"];
            if (callerId.Contains("Microsoft.CognitiveServices"))
            {
                var parts = callerId.Split('/');
                return (parts.Length > 10) ? parts[8] + "/" + parts[10] : "N/A";
            }
            if (callerId.Contains("Microsoft.ManagedIdentity"))
            {
                var parts = callerId.Split('/');
                return (parts.Length > 8) ? parts[8] : "N/A";
            }
            return context.Subscription.Id;
        }" />
        <set-variable name="foundry-name" value="@{
            var callerId = (string)context.Variables["caller-resource-id"];
            if (callerId.Contains("Microsoft.CognitiveServices"))
            {
                var parts = callerId.Split('/');
                return (parts.Length > 10) ? parts[10] : "N/A";
            }
            return "N/A";
        }" />
        <!-- Set custom header with Project Name for gateway logging (will be captured in ApiManagementGatewayLogs.RequestHeaders) -->
        <set-header name="x-caller-name" exists-action="override">
            <value>@((string)context.Variables["caller-name"])</value>
        </set-header>
        <set-header name="x-caller-id" exists-action="override">
            <value>@((string)context.Variables["caller-resource-id"])</value>
        </set-header>
        <set-header name="x-foundry-name" exists-action="override">
            <value>@((string)context.Variables["foundry-name"])</value>
        </set-header>
        <!-- CORS policy to allow Azure AI Foundry portal -->
        <cors allow-credentials="true">
            <allowed-origins>
                <origin>https://ai.azure.com</origin>
            </allowed-origins>
            <allowed-methods preflight-result-max-age="300">
                <method>*</method>
            </allowed-methods>
            <allowed-headers>
                <header>*</header>
            </allowed-headers>
            <expose-headers>
                <header>*</header>
            </expose-headers>
        </cors>
        <!-- Authenticate to Azure OpenAI with API Management's managed identity -->
        <authentication-managed-identity resource="https://cognitiveservices.azure.com" output-token-variable-name="managed-id-access-token" ignore-error="false" />
        <set-header name="Authorization" exists-action="override">
            <value>@("Bearer " + (string)context.Variables["managed-id-access-token"])</value>
        </set-header>
        <set-backend-service backend-id="{backend-id}" />
        <!--Policy 2 - Emit the Azure OpenAI Token Metrics to Application Insights -->
        <!-- Note: Azure Monitor limits to 10 dimension keys per metric, 5 are used by default (Region, Service ID/Name/Type) -->
        <!-- Custom dimensions (max 5): Project ID, Caller Name, API ID, User ID -->
        <!-- For model/deployment tracking, use ApiManagementGatewayLlmLog which has DeploymentName natively -->
        <azure-openai-emit-token-metric namespace="openai">
            <dimension name="Caller ID" value="@((string)context.Variables["caller-id"])" />
            <dimension name="Caller Name" value="@((string)context.Variables["caller-name"])" />
            <dimension name="Foundry Name" value="@((string)context.Variables["foundry-name"])" />
            <dimension name="API ID" value="@(context.Api.Id)" />
            <dimension name="User ID" value="@(context.Request.Headers.GetValueOrDefault("x-user-id", "N/A"))" />
        </azure-openai-emit-token-metric>
    </inbound>
    <backend>
        <!--Policy 1 - Apply load-balancing and retry mechanisms -->
        <!--Set count to one less than the number of backends in the pool to try all backends until the backend pool is temporarily unavailable.-->
        <retry count="{retry-count}" interval="0" first-fast-retry="true" condition="@(context.Response.StatusCode == 429 || (context.Response.StatusCode == 503 &amp;&amp; !context.Response.StatusReason.Contains("Backend pool") &amp;&amp; !context.Response.StatusReason.Contains("is temporarily unavailable")))">
            <forward-request buffer-request-body="true" />
        </retry>
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
        <choose>
            <!--Return a generic error that does not reveal backend pool details.-->
            <when condition="@(context.Response.StatusCode == 503)">
                <return-response>
                    <set-status code="503" reason="Service Unavailable" />
                </return-response>
            </when>
        </choose>
    </on-error>
</policies>
