[project]
name = "bulk-insert"
version = "0.1.0"
description = "Create book Index and upload documents in bulk to Azure AI Search"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "azure-identity>=1.25.1",
    "azure-search-documents>=11.6.0",
    "httpx>=0.27.0",
]
