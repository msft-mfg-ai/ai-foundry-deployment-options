# Option: AI Gateway (Existing External APIM) with Foundry Standard (VNET)

> **Unified architecture note:** This placeholder option should align with the unified APIM gateway architecture when expanded: `policy-per-model.xml`, `caller-identity`, `per-model-routing`, `FOUNDRY_INSTANCES_JSON`, and per-`(instance, model, location)` backends.
