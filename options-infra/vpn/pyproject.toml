[project]
name = "foundry-wireguard-vpn"
version = "0.1.0"
description = "azd orchestration for a routed WireGuard VPN attached to an existing Foundry VNet"
requires-python = ">=3.11"
dependencies = [
  "cryptography>=45.0.0",
]

[tool.uv]
package = false
