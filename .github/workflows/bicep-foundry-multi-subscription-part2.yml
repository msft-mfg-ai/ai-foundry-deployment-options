name: 'Bicep: foundry-multi-subscription-parts (part2)'

on:
  push:
    branches: [main]
    paths:
      - 'options-infra/foundry-multi-subscription-parts/part2.bicep'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'
  pull_request:
    branches: [main]
    paths:
      - 'options-infra/foundry-multi-subscription-parts/part2.bicep'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'

jobs:
  validate:
    uses: ./.github/workflows/bicep-validate-reusable.yml
    with:
      bicep-file: part2.bicep
      working-directory: options-infra/foundry-multi-subscription-parts
