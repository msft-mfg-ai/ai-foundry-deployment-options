name: 'Bicep: option_ai-gateway-pe'

on:
  push:
    branches: [main]
    paths:
      - 'options-infra/option_ai-gateway-pe/main.bicep'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'
  pull_request:
    branches: [main]
    paths:
      - 'options-infra/option_ai-gateway-pe/main.bicep'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'

jobs:
  validate:
    uses: ./.github/workflows/bicep-validate-reusable.yml
    with:
      bicep-file: main.bicep
      working-directory: options-infra/option_ai-gateway-pe
