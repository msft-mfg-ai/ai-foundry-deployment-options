name: 'Bicep: foundry-multi-subscription-parts (part0)'

on:
  push:
    branches: [main]
    paths:
      - 'options-infra/foundry-multi-subscription-parts/part0.bicep'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'
  pull_request:
    branches: [main]
    paths:
      - 'options-infra/foundry-multi-subscription-parts/part0.bicep'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'

jobs:
  validate:
    uses: ./.github/workflows/bicep-validate-reusable.yml
    with:
      bicep-file: part0.bicep
      working-directory: options-infra/foundry-multi-subscription-parts
