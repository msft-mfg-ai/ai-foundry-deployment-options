name: 'Bicep: ai-gateway-anthropic'

on:
  push:
    branches: [main]
    paths:
      - 'options-infra/ai-gateway-anthropic/**'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'
  pull_request:
    branches: [main]
    paths:
      - 'options-infra/ai-gateway-anthropic/**'
      - 'options-infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'

jobs:
  validate:
    uses: ./.github/workflows/bicep-validate-reusable.yml
    with:
      bicep-file: main.bicep
      working-directory: options-infra/ai-gateway-anthropic
